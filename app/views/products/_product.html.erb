<% cache("#{product}/#{product.orders_count}") do %>
<div class="product">
  <div class="col-md-4">
    <div class="thumbnail">
      <%= image_tag product.image.thumb, alt: product.title, class: "img-responsive" %>
      <div class="caption">
        <h3 class="f18"><%= product.title %></h3>
        <p>订购数：<span class="green f20"><em><%= product.orders_count %></em></span></p>
        
        
        <div class="row">
          <div class="col-md-6">
            <p class="price f16">
              <span class="green f24"><%= product.origin_price %></span>&nbsp;元/斤
            </p>
            <!--div class="weight-input-control">
              <a class="adds-on">-</a>
              <input type="text" name="weight" value="1.0" />
              <a class="adds-on">+</a>
              斤
            </div-->
            <!--div class="input-group">
              <a class="input-group-addon" data-id="<%= product.id %>" data-type="-" onclick="App.doWeight(this);">-</a>
              <input onkeyup="App.checkValue(this);" onafterpaste="App.checkValue(this);" type="text" id="product-weight-<%= product.id %>" class="form-control" value="1" style="text-align:center;">
              <a class="input-group-addon" data-id="<%= product.id %>" data-type="+" onclick="App.doWeight(this);">+</a>
            </div-->
              
          </div>
          <div class="col-md-6">
            <div class="text-center">
              <%= link_to "立即预订", new_product_order_path(product), class: "btn btn-success", role: "button" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>