<% content_for :mobile_title do %>
<%= @mobile_title %>
<% end %>

<%= render "layouts/mobile_navbar", left: "back", right: "home", title: "" %>

<div class="container">
  <div class="orders" id="mobile-orders">
    <% if @orders.empty? %>
    暂无订单
    <% else %>
      <% @orders.each do |order| %>
        <%= render "orders/mobile_order", order: order %>
      <% end %>
    <% end %>
  </div>
</div>